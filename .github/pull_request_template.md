**What does this change?**

**Why?**

**Checklist**

- [ ] `npm test` passes
- [ ] `npm run format` has been run
- [ ] If this touches `src/ui/panel.js` or fonts, I've manually verified it in a real browser (jsdom tests cover behavior, not visual layout — see `docs/ARCHITECTURE.md`)
- [ ] If this changes scope (what PrefKeeper does/doesn't do), I've checked it against `docs/SCOPE.md`
